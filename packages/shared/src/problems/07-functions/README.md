# Ch07. 関数

関数宣言、 引数、 `return`、 スコープの入り口。 ここから採点モデルが S3 で stdout から function へ切り替わるため、 「自分で関数を作って返す」 を本格的に練習する。

## 含まれるステージ

- **S2** 文法定着 — `function` 宣言とアロー関数、 引数 / `return` / デフォルト引数。 末尾に **チャレンジ** (成績判定)。
- **S3** ロジック入門 — 引数を受け取って結果を返す関数 (function 採点)。
- **S4** アルゴリズム — ヘルパ関数を定義してロジックを分割する。
- **S5** 設計演習 — 複数関数を組み合わせて 1 つの仕様を実装する。

## MDN 既定ページ

- [関数](https://developer.mozilla.org/ja/docs/Web/JavaScript/Guide/Functions)

## ディレクトリ構成

```
07-functions/
├── README.md
├── _index.ts
├── s2/   # Phase 7 で追加済み (16 問、 末尾はチャレンジ問題 成績判定)
├── s3/   # Phase 8 で追加済み (8 問)
├── s4/   # Phase 9 で追加済み (5 問、 末尾は卒業課題)
└── s5/   # Phase 9 で追加済み (3 問、 末尾は S5 卒業課題 makeStore)
```

## S2 で扱う問題 (16 問、 16 番目がチャレンジ問題)

| # | ID | 主題 |
|---|---|---|
| 01 | `S2-Ch07-01-function-greet` | function 宣言の基本 |
| 02 | `S2-Ch07-02-function-add` | 引数と return |
| 03 | `S2-Ch07-03-arrow-basic` | アロー関数の暗黙 return |
| 04 | `S2-Ch07-04-arrow-with-args` | アロー関数で 2 引数 |
| 05 | `S2-Ch07-05-default-args` | デフォルト引数 |
| 06 | `S2-Ch07-06-early-return` | 早期 return |
| 07 | `S2-Ch07-07-no-return-undefined` | return しないと undefined |
| 08 | `S2-Ch07-08-call-twice` | 同じ関数を 2 回呼ぶ |
| 09 | `S2-Ch07-09-pass-string` | 文字列引数を加工 |
| 10 | `S2-Ch07-10-pass-array` | 配列引数で先頭を返す |
| 11 | `S2-Ch07-11-nested-call` | 関数の中から関数を呼ぶ |
| 12 | `S2-Ch07-12-multiple-returns` | 複数の return で分岐 |
| 13 | `S2-Ch07-13-helper-function` | ヘルパー関数で重複を排除 |
| 14 | `S2-Ch07-14-arrow-block` | アロー関数で { } 本体 |
| 15 | `S2-Ch07-15-function-loop-sum` | 関数の中で for ループ |
| 16 | `S2-Ch07-16-grading-capstone` ⭐ | **[チャレンジ]** 成績判定 |

### S2 学習ポイント

- `function 名前() { ... }` で宣言。 宣言しただけでは実行されない。 `名前()` で呼び出して初めて中身が動く
- `return` で値を返す。 書かないと暗黙的に `undefined` が返る
- アロー関数 `() => 値` は暗黙 return、 `() => { return 値; }` は明示 return
- デフォルト引数 `f(x = 既定値)` で省略可能にできる
- 早期 return で if/else のネストを浅く保つ
- 関数を **小さな部品** として分割すれば、 後から組み合わせて複雑な処理を作れる

## S3 で扱う問題 (8 問)

| # | ID | 主題 |
|---|---|---|
| 01 | `S3-Ch07-01-default-greet` | デフォルト引数 `name = "ゲスト"` |
| 02 | `S3-Ch07-02-make-multiplier` | 関数を返す関数 (クロージャ入口) |
| 03 | `S3-Ch07-03-apply-twice` | 関数を引数で受けて 2 回適用 |
| 04 | `S3-Ch07-04-sum-rest` | 残余パラメータ `...nums` |
| 05 | `S3-Ch07-05-clamp` | min/max にクランプする |
| 06 | `S3-Ch07-06-call-n-times` | 関数を n 回呼んで結果を集める |
| 07 | `S3-Ch07-07-negate-predicate` | 真偽反転の高階関数 |
| 08 | `S3-Ch07-08-compose` | 関数合成 (f ∘ g) |

### S3 学習ポイント

- 関数を **引数で受ける** / **戻り値で返す** の両方を扱い、 第一級関数を体感する
- デフォルト引数と残余パラメータを使い分ける
- クロージャは「内側の関数が外側の変数を覚える」 性質。 `makeMultiplier` で実感できる

## S4 で扱う問題 (5 問、 5 番目が卒業課題)

| # | ID | 主題 |
|---|---|---|
| 01 | `S4-Ch07-01-once` | 1 度だけ実行する高階関数 (`once`) |
| 02 | `S4-Ch07-02-memoize` | Map で結果をキャッシュする高階関数 (`memoize`) |
| 03 | `S4-Ch07-03-pipe` | 残余引数で受けた関数を左から順に適用 (`pipe`) |
| 04 | `S4-Ch07-04-curry2` | 2 引数関数のカリー化 (`curry`) |
| 05 | `S4-Ch07-05-validator-capstone` ⭐ | **[卒業課題]** 検証関数の合成 (`combineChecks`) |

### S4 学習ポイント

- **高階関数の自作**: `once` / `memoize` / `curry` のような汎用ヘルパを 「自分の手で」 書けるようになる
- **クロージャでの状態保持**: 「呼ばれたか」 や Map をクロージャに閉じ込めて、 返した関数に状態を持たせる
- **可変長合成**: 残余引数 `(...fns)` で関数列を受け、 `reduce` で順番に適用する
- **関数の分割と合成**: 大きな処理を 「**小さな 1 引数関数**」 と 「**それを束ねる高階関数**」 に分けて構成する (卒業課題のテーマ)
- **早期 return の活用**: 検証ループのように 「最初に見つかったものを即座に返す」 形を組み立てる

## S5 で扱う問題 (3 問、 3 番目が S5 卒業課題)

| # | ID | 主題 |
|---|---|---|
| 01 | `S5-Ch07-01-decompose-stats` | 統計計算を `mean` / `variance` / `stdDev` / `summarize` の 4 つの純粋関数に分割し、 後段が前段を再利用する設計分割 |
| 02 | `S5-Ch07-02-run-steps` | 任意個数のステップ関数を左から繋ぐ runSteps。 `{ ok, value }` で成功、 `{ ok, error }` で失敗の早期離脱を表現する高階関数 |
| 03 | `S5-Ch07-03-make-store-capstone` ⭐ | **[S5 卒業課題]** state と `Set<listener>` を 1 つのクロージャに閉じ込め、 `getState` / `setState` (更新関数を受ける高階関数) / `subscribe` (解除関数を返す) / `getListenerCount` の 4 つを返す Redux 風ミニストア |

### S5 学習ポイント

- **関数分割と純粋関数による設計**: 「1 つの巨大関数で全部やる」 のではなく、 役割の違う複数の純粋関数に切り分けて協調させる。 後段の関数が前段の関数の戻り値を再利用することで、 修正の波及範囲が小さくなる
- **高階関数の応用**: 関数を引数で受けて関数を返す形を、 ステップ列 (`runSteps`) や 状態更新 (`setState((prev) => next)`) の API として実用する
- **早期離脱の組み立て**: 「失敗が確定した瞬間にその場で return」 を、 連鎖ステップに一般化する。 S4 `combineChecks` で身につけたパターンを、 「値も流す」 形に拡張する
- **クロージャを 1 つの API として扱う**: 複数の関数が **共通のクロージャ変数 (state / listener 集合)** を見ながら役割分担する。 Ch07 までで学んだ 「関数を返す関数」 を、 「**オブジェクトに包んだ複数の関数を返す関数**」 に発展させる
- **設計判断**: 「listener を配列で持つか Set で持つか」 「updater は値か関数か」 など、 API の細部に表れる選択肢を意識して決める

## 状態

S2: 16 問、 S3: 8 問、 S4: 5 問、 S5: 3 問追加済み (Phase 7 / Phase 8 / Phase 9)。 Ch07 関数 章は完成。
