---
id: 7-1-3
title: コンストラクタとthis
takeaway: "コンストラクタはnewのときに動く初期化処理。thisは自分自身を指す"
introduces: [コンストラクタ, this]
requires: [クラス, new, プロパティ, 引数, インスタンス]
header: "TypeScript入門研修"
---

<!-- _class: lead -->

# 7-1-3
# コンストラクタとthis

TypeScript入門研修 — Module 7 / レッスン7-1

<!-- ノート: レッスン7-1の山場です。クラスを実用にするための、いちばん重要な仕組みを扱います。 -->

---

## なぜ必要か

- 作ってから1つずつ代入するのでは、書き漏らしが起きる
- 商品ごとに違う値を、作るときに渡したい

<!-- ノート: つかみ。7-1-2の書き方だと、new してから item.name = ... と何行も書くことになる。3-3-4で見た「作るときの書き漏らし」がクラスでも起きる。 -->

---

## 結論

**コンストラクタは`new`のときに動く初期化処理。`this`は自分自身を指す**

```
constructor(引数: 型) {
  this.プロパティ名 = 引数;
}
```

<!-- ノート: 結論を先に言い切る。2語を定義する。コンストラクタは「構築するもの」の意味。thisは「いま作られようとしているインスタンス」を指す特別な名前。 -->

---

## 最小のコード

```ts
class Product {
  name: string;
  price: number;

  constructor(name: string, price: number) {
    this.name = name;
    this.price = price;
  }
}

const item = new Product("コーヒー", 480);
console.log(item.name); // => "コーヒー"
```

<!-- ノート: newのかっこに渡した値が、そのままコンストラクタの引数になる。this.name がプロパティ、右辺の name が引数。同じ名前だが別物である点を必ず指して説明する。初期値なしでもエラーが出なくなる。 -->

---

## newで実物ができるまで

![w:950](assets/constructor-flow.svg)

<!-- ノート: newが呼ばれる → 空のインスタンスができる → コンストラクタが動いてプロパティが埋まる、という順番。thisはその「空のインスタンス」を指している。this.を書き忘れると、ただのローカル変数への代入になって何も起きない。 -->

---

<!-- _class: summary -->

## まとめ

**コンストラクタは`new`のときに動く初期化処理。`this`は自分自身を指す**

<!-- ノート: 結論の再掲だけ。データは持てたので、次は処理を持たせると予告して締める。 -->
