# HTML/CSS 入門 カリキュラム

SES 未経験者向けの HTML/CSS 入門講座です。1 トピック = 覚えることが 1 つ(takeaway 1 文)の粒度で、
ページの中身を作る HTML と、見た目を決める CSS を、文書の骨格から余白・配置・読みやすさまで通します。
構成は [MDN Learn web development の Core](https://developer.mozilla.org/ja/docs/Learn_web_development/Core) の本経路に合わせています。

- 前提講座: **ITのきほん(`it-basics`)**。スキルツリーの入口講座で、コンピュータ・ファイル・Web の基本のことばをそろえてから受講します(`course.json` の `prerequisites` に対応)。ITのきほんから出る本土の枝の 1 本 (もう 1 本は SQL 入門)
- 後続: フロントエンド本線は **JavaScript 入門(`javascript-basics`)** → **TypeScript 入門(`typescript-basics`)**。[Frontend Developer Roadmap](https://roadmap.sh/frontend) と同じ順。見た目の深化は **モダンCSS 入門(`modern-css-basics`)** へ続く。Git 入門は JavaScript のあとにフロントの扇へ出る共通スキルで、この講座の直後の後続ではない

## 終了時の目標

終了時、受講者はテキストエディタとブラウザーだけで 1 枚の静的ページを組み立てられます。

- HTML で見出し階層・リスト・リンク・代替テキスト付き画像・`label` 付き入力と `button` を置ける
- CSS を別ファイルと class で当て、余白(`padding` / `margin` / `gap`)と flex で箱を並べられる
- 文字サイズ・行間・コントラストを、読める状態にできる
- 開発者ツールで「どの宣言が当たっているか」を見て、効かない指定を順序・詳細度・継承のどれで直すかを判断できる

## 進め方と範囲

- Web(LMS): スライド視聴・まとめ・確認クイズ(合格点 80)
- 手を動かす部分: `practice.md` の「手元で試す」。テキストエディタで書いて、ブラウザーで開いて確かめる
- コード演習(VS Code 拡張の採点)は **この講座にはありません**。見た目の採点は現時点の採点基盤の対象外です

対象外:

- JavaScript / DOM / フォーム送信の処理
- CSS フレームワーク、ビルドツール
- MDN Core の追加チュートリアル(SVG、iframe、段組み、カスケードレイヤー、レガシーレイアウトなど)
- 本格的なレスポンシブデザイン(複数ブレークポイントの設計、コンテナクエリ、多数の端末での検証)。
  **`viewport` の `meta` と、幅 1 本の `@media` は範囲に含みます**

## 全体構成(6 モジュール / 22 レッスン / 83 トピック)

### M0. オリエンテーション — レッスン0-1 Webページのしくみ

| ID | トピック | takeaway |
| --- | --- | --- |
| 0-1-1 | Webページはブラウザーが組み立てる | ブラウザーは、受け取ったファイルを読んで画面を組み立てている |
| 0-1-2 | HTMLはページの中身を書く | HTMLは、ページに何を置くかを書くための言語 |
| 0-1-3 | CSSは見た目を決める | CSSは、HTMLで置いたものの見た目を決めるための言語 |
| 0-1-4 | 保存して開けばすぐ試せる | HTMLファイルは保存してブラウザーで開けば、その場で結果を確認できる |

### M1. 文書の骨格 — レッスン1-1 要素とタグ

| ID | トピック | takeaway |
| --- | --- | --- |
| 1-1-1 | タグで囲んで要素を作る | HTMLは、開始タグと終了タグで中身を囲んで要素を作る |
| 1-1-2 | 属性でタグに情報を足す | 属性は開始タグの中に書き、その要素に追加の情報を与える |
| 1-1-3 | HTMLファイルの決まった骨組み | HTMLファイルは、headとbodyを持つ決まった骨組みから書き始める |
| 1-1-4 | 要素は入れ子にできる | 要素の中に要素を入れると、親子の構造ができる |
| 1-1-5 | 骨組みの宣言は不具合を防いでいる | doctype・charset・lang・ページ名の宣言は、それぞれ決まった不具合を防いでいる |

### M1. 文書の骨格 — レッスン1-2 文章とリンク

| ID | トピック | takeaway |
| --- | --- | --- |
| 1-2-1 | 見出しはh1〜h6で階層を作る | 見出しはh1〜h6の番号で、内容の階層を表す |
| 1-2-2 | 段落はpで区切る | 文章のひとかたまりは、pで1段落ずつ区切る |
| 1-2-3 | リストは箇条書きを表す | 並んだ項目はulまたはolの中にliを並べて表す |
| 1-2-4 | リンクはaのhrefでつなぐ | aのhref属性に行き先を書くと、クリックで移動できるリンクになる |
| 1-2-5 | 見出しの番号は飛ばさない | 見出しはh1から順に使い、番号を飛ばさない |

### M1. 文書の骨格 — レッスン1-3 ページの領域

| ID | トピック | takeaway |
| --- | --- | --- |
| 1-3-1 | ページの大きな領域に名前を付ける | ページの上下や本文といった大きな領域は、専用の要素で名前を付ける |
| 1-3-2 | 本文の区切りはsectionとarticleで示す | 本文の中の区切りは、単独で成り立つならarticle、章の一区切りならsectionで囲む |
| 1-3-3 | 本筋から外れる補足はasideで囲む | 本筋から外れていて、無くても本文が成り立つ内容はasideで囲む |

### M1. 文書の骨格 — レッスン1-4 強調と意味のある語句

| ID | トピック | takeaway |
| --- | --- | --- |
| 1-4-1 | emは読み方が変わる強調 | 文の中で読み方が変わるほど強めたい語句はemで囲む |
| 1-4-2 | strongは重要さの印 | 読み飛ばされると困るほど重要な語句はstrongで囲む |
| 1-4-3 | 見た目だけ変えたいならCSSで変える | 太字や斜体にしたいだけなら、意味の印ではなくCSSで見た目を変える |
| 1-4-4 | 用語と説明の組はdlで表す | 用語とその説明が組になる並びは、dlの中にdtとddを並べて表す |

### M2. メディアとフォーム — レッスン2-1 画像を載せる

| ID | トピック | takeaway |
| --- | --- | --- |
| 2-1-1 | 画像はimgのsrcで貼る | 画像はimgタグのsrc属性にファイルの場所を書いて表示する |
| 2-1-2 | altは画像の代わりになる文 | alt属性には、画像が見えないときに代わりに伝わる文を書く |
| 2-1-3 | 相対パスでファイルの場所を指す | 相対パスは、いま書いているファイルから見た位置でファイルを指す |
| 2-1-4 | widthとheightで場所を先に確保する | imgにwidthとheight属性を書くと、読み込み前から表示場所が確保される |

### M2. メディアとフォーム — レッスン2-2 入力を受け取る

| ID | トピック | takeaway |
| --- | --- | --- |
| 2-2-1 | inputは入力欄を作る | inputはtype属性で種類が変わる、1つの入力欄を作るタグ |
| 2-2-2 | labelは入力欄と説明を結び付ける | labelのforとinputのidを同じ値にすると、説明と入力欄が結び付く |
| 2-2-3 | buttonは押せる部品を作る | buttonは、利用者が押して操作を始めるための部品を作る |
| 2-2-4 | typeは入力の種類に合わせて選ぶ | typeは見た目ではなく、受け取りたい入力の種類に合わせて選ぶ |
| 2-2-5 | formでひとまとまりにして送る | 1回で送りたい入力欄はformで囲み、中のbuttonが送る部品になる |

### M2. メディアとフォーム — レッスン2-3 動画と音声の基本

| ID | トピック | takeaway |
| --- | --- | --- |
| 2-3-1 | 動画はvideoに埋め込む | 動画はvideoのsrcにファイルを指定し、controlsで操作できるようにする |
| 2-3-2 | 音声はaudioに埋め込む | 音声はaudioのsrcにファイルを指定し、videoと同じ書き方で埋め込む |
| 2-3-3 | trackで字幕を付ける | 動画の内容を音に頼らず伝えるには、trackで字幕のファイルを添える |

### M2. メディアとフォーム — レッスン2-4 表でデータを示す

| ID | トピック | takeaway |
| --- | --- | --- |
| 2-4-1 | 表は行と列でデータを並べる | 表はtableの中にtrで行を作り、その中にtdでセルを並べて作る |
| 2-4-2 | 見出しのセルはthで示す | その行や列が何を表すかを示すセルは、tdではなくthで書く |
| 2-4-3 | captionで表に題名を付ける | その表が何の表かは、tableの先頭に置いたcaptionで示す |

### M3. CSS の当て方 — レッスン3-1 CSSを書いて読み込む

| ID | トピック | takeaway |
| --- | --- | --- |
| 3-1-1 | CSSはセレクタと宣言で書く | CSSは「どれに」を表すセレクタと、「何を」を表す宣言の組で書く |
| 3-1-2 | CSSは別ファイルにしてlinkで読み込む | CSSは.cssファイルに分け、headのlinkで読み込む |
| 3-1-3 | classで当てる相手を選ぶ | class属性を付けると、同じ見た目を必要な要素にだけ当てられる |
| 3-1-4 | CSSの当て方は3通りある | CSSはstyle属性・style要素・外部ファイルの3通りで当てられ、既定は外部ファイル |

### M3. CSS の当て方 — レッスン3-2 どの指定が効くのか

| ID | トピック | takeaway |
| --- | --- | --- |
| 3-2-1 | 同じ相手に当たると後の宣言が勝つ | 同じ条件で同じプロパティを指定したら、後に書いた宣言が勝つ |
| 3-2-2 | 詳細度が高いセレクタが優先される | 書いた順より先に、より細かく指定したセレクタの宣言が優先される |
| 3-2-3 | 文字の指定は子に受け継がれる | 文字にかかわる一部のプロパティは、親から子へ受け継がれる |
| 3-2-4 | important は最後の手段 | !importantは順序も詳細度も飛び越えて勝つので、最後の手段にする |

### M3. CSS の当て方 — レッスン3-3 セレクタと状態

| ID | トピック | takeaway |
| --- | --- | --- |
| 3-3-1 | セレクタは要素・class・idで選ぶ | 当てる相手は、要素名・class・idの3つの書き分けで選ぶ |
| 3-3-2 | 空白でつなぐと中の要素だけに当たる | セレクタを空白でつなぐと、その中にある要素だけに当たる |
| 3-3-3 | 擬似クラスは状態で当たる | コロンで始まる擬似クラスを付けると、その要素が特定の状態のときだけ当たる |

### M4. 箱と配置 — レッスン4-1 ボックスと余白

| ID | トピック | takeaway |
| --- | --- | --- |
| 4-1-1 | 要素は四角い箱として置かれる | すべての要素は、幅と高さを持つ四角い箱として置かれる |
| 4-1-2 | paddingは箱の内側の余白 | paddingは、ボーダーと中身のあいだに空ける内側の余白 |
| 4-1-3 | marginは箱の外側の余白 | marginは、ボーダーの外側に空ける、隣とのあいだの余白 |
| 4-1-4 | box-sizingで幅の数え方を揃える | box-sizing: border-box にすると、widthがボーダーまで含めた幅になる |
| 4-1-5 | borderは太さ・線種・色をまとめて書く | ボーダーはborderに太さ・線種・色の3つを空白で並べて指定する |

### M4. 箱と配置 — レッスン4-2 並べ方

| ID | トピック | takeaway |
| --- | --- | --- |
| 4-2-1 | ブロックは縦、インラインは横に並ぶ | 要素にはブロックとインラインがあり、既定の並ぶ向きが違う |
| 4-2-2 | display:flexで子を横に並べる | 親にdisplay: flexを指定すると、子の要素が横に並ぶ |
| 4-2-3 | gapで並べた箱のすき間を空ける | gapは、並べた子どうしのすき間だけをまとめて空ける |
| 4-2-4 | Flexの子は主軸に沿って縮む | flexの子は既定で主軸に沿って1行に並び、収まらなければ縮む |
| 4-2-5 | margin autoで箱を中央に置く | 幅を決めたブロックにmargin: 0 autoを指定すると、左右中央に置かれる |

### M4. 箱と配置 — レッスン4-3 Flexの揃えと折り返し

| ID | トピック | takeaway |
| --- | --- | --- |
| 4-3-1 | justify-contentは主軸の寄せ方 | justify-contentは、フレックスアイテムを主軸のどこに寄せるかを決める |
| 4-3-2 | align-itemsは交差軸の揃え方 | align-itemsは、フレックスアイテムを交差軸のどこに揃えるかを決める |
| 4-3-3 | flex-wrapで折り返しを許す | flex-wrap: wrapを指定すると、1行に収まらないアイテムが次の行へ送られる |

### M4. 箱と配置 — レッスン4-4 Gridの基本

| ID | トピック | takeaway |
| --- | --- | --- |
| 4-4-1 | display gridは格子に並べる | 親にdisplay: gridとgrid-template-columnsを書くと、子が決めた列数の格子に並ぶ |
| 4-4-2 | frは余った幅を配る単位 | 列幅にfrを使うと、余った幅がその比率で配られる |
| 4-4-3 | grid-columnで複数の列にまたがる | grid-columnにspanを書くと、そのアイテムが複数の列にまたがる |

### M4. 箱と配置 — レッスン4-5 位置指定と浮動

| ID | トピック | takeaway |
| --- | --- | --- |
| 4-5-1 | position relativeは元の位置からずらす | position: relativeを指定すると、場所を保ったまま元の位置からずらせる |
| 4-5-2 | position absoluteは基準の箱に重ねる | position: absoluteは通常フローから抜け、relativeを持つ先祖を基準に置かれる |
| 4-5-3 | floatは文章を回り込ませる | floatを指定すると、その要素が左右に寄り、後ろの文章が回り込む |

### M4. 箱と配置 — レッスン4-6 画面幅に合わせる(最小)

| ID | トピック | takeaway |
| --- | --- | --- |
| 4-6-1 | viewportの指定で実際の幅に合わせる | headにviewportのmetaを書くと、スマートフォンが実際の画面幅で表示する |
| 4-6-2 | max-widthで伸び縮みさせる | widthの代わりにmax-widthを使うと、狭い画面でははみ出さずに縮む |
| 4-6-3 | メディアクエリーで幅ごとに切り替える | メディアクエリーを1本書くと、決めた幅を境に別の宣言へ切り替えられる |

### M5. 読みやすさ — レッスン5-1 読める画面にする

| ID | トピック | takeaway |
| --- | --- | --- |
| 5-1-1 | 本文の文字サイズは16px前後にする | 本文のfont-sizeは16px前後を基準にし、小さくしすぎない |
| 5-1-2 | 行間を空けると文章が読みやすい | line-heightを1.5〜1.8にすると、長い文章が読みやすくなる |
| 5-1-3 | 文字色と背景色はコントラストで決める | 文字色と背景色は、明るさの差が十分にある組み合わせを選ぶ |
| 5-1-4 | 仕上げは開発者ツールで確かめる | ブラウザーの開発者ツールを使うと、当たっているCSSをその場で確かめられる |
| 5-1-5 | 効かない指定は5つの原因に切り分ける | 効かない宣言は、書き方・セレクタ・順序・詳細度・継承のどれが原因かを開発者ツールで確かめて直す |

### M5. 読みやすさ — レッスン5-2 フォントと文字の装飾

| ID | トピック | takeaway |
| --- | --- | --- |
| 5-2-1 | font-familyは候補を並べて書く | font-familyは使いたい書体から順に候補を並べ、最後に総称名で締める |
| 5-2-2 | font-weightで文字の太さを決める | font-weightは文字の太さを決め、太字にしたいだけのときはこれを使う |
| 5-2-3 | text-alignは行の寄せ方 | text-alignは箱の中で行をどちらに寄せるかを決める |

### M5. 読みやすさ — レッスン5-3 リストとリンクの見た目

| ID | トピック | takeaway |
| --- | --- | --- |
| 5-3-1 | list-styleでマーカーを変える | リストの先頭に付く印は、list-style-typeで種類を変えられる |
| 5-3-2 | リンクは状態の順番どおりに書く | リンクの状態を指定するときは、link・visited・hover・focus・activeの順に書く |
| 5-3-3 | 下線を消すなら別の手がかりを残す | text-decorationで下線を消すときは、色以外の手がかりを必ず残す |

### M5. 読みやすさ — レッスン5-4 単位・背景・はみ出し

| ID | トピック | takeaway |
| --- | --- | --- |
| 5-4-1 | 相対的な単位は基準を持つ | em・rem・%は、それぞれ何を基準にするかが決まっている |
| 5-4-2 | 背景の画像はsizeで収め方を決める | background-imageで敷いた画像は、background-sizeで収め方を決める |
| 5-4-3 | はみ出した中身はoverflowで決める | 箱に収まらない中身をどう扱うかは、overflowで決める |

## この講座の進め方

- 各レッスンは **スライド → まとめ → 確認クイズ** の順で並びます
- `practice.md` の「手元で試す」はテキストエディタとブラウザーだけで完結します。自動採点はありません
- スマートフォンからでも、スライドとまとめと確認クイズだけで最後まで進められます

## 今後の拡張(この講座の外)

- VS Code 拡張で starter の `.html` / `.css` を開くだけの導線(採点なし)
- 見た目の自動採点(プレビュー一致)。採点基盤の整備が先

## クレジット(原典)

この講座は、次の公開ドキュメントを参照して未経験者向けに順序・粒度を再設計した独自教材です。
翻訳・転載ではありません。原典への言及はこのファイルに集約し、スライド・`doc.md` 本文には書きません。

| 役割 | 正本 | ライセンス |
| --- | --- | --- |
| 骨格 | [MDN ウェブ開発の学習](https://developer.mozilla.org/ja/docs/Learn_web_development) | CC-BY-SA 2.5 |
| 日本語本文 | [mdn/translated-content](https://github.com/mdn/translated-content) の `files/ja/` | CC-BY-SA 2.5 |
| 英語正本 | [mdn/content](https://github.com/mdn/content) | CC-BY-SA 2.5 |
| 学習順の参照 | [mdn/curriculum](https://github.com/mdn/curriculum) | CC-BY-SA 2.5 |
