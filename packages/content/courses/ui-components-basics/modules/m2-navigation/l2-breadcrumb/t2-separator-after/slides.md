---
id: 2-2-2
title: 区切り記号は::afterで足す
takeaway: "パンくずの区切り記号は文字として書かず、::afterで表示だけ足す"
introduces: ["::after"]
requires: [パンくずリスト]
header: "UI部品入門"
---

<!-- _class: lead -->

# 2-2-2
# 区切り記号は::afterで足す

UI部品入門 — Module 2 / レッスン2-2

<!-- ノート: 2段階目のレイアウトです。横に並べ、間に区切り記号を置きます。 -->

---

## なぜ必要か

- 飾りの記号が本文に混ざり、道のりの文字と区別が付かない
- 記号を変えたくなったとき、全ページの HTML を直すことになる

<!-- ノート: つかみ。見た目のためだけの文字をHTMLに置くと、意味と見た目が絡まります。 -->

---

## 結論

**パンくずの区切り記号は文字として書かず、`::after`で表示だけ足す**

- `::after` … 要素の後ろに表示だけを足す書き方
- HTML には道のりの文字しか残らない

<!-- ノート: 結論。見た目のためだけの文字は、CSS 側に置くのが原則です。 -->

---

## 最小のコード

```css
.breadcrumb li:not(:last-child)::after {
  content: "/";
  margin-inline: 8px;
  color: var(--muted);
}
```

<!-- ノート: :not(:last-child)で、最後の項目のうしろには付けないようにしています。 -->

---

## 読み上げは保証されない

- `content` の文字を読み上げる環境もある
- 消したいなら `aria-hidden` を付けた要素で区切る
- ここで確実に良くなるのは **HTML の整理** のほう

<!-- ノート: 失敗例の枠。「CSSに置いたから読まれない」と覚えないよう、ここははっきり言います。 -->

---

<!-- _class: summary -->

## まとめ

**パンくずの区切り記号は文字として書かず、`::after`で表示だけ足す**

<!-- ノート: 結論の再掲だけ。次はトークンで濃さを付けます。 -->
