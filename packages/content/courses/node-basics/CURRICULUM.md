# Node.js 入門 — カリキュラム構成 (準備中)

> **状態: 準備中。** スキルツリー上の位置と学習範囲を先に確保しているプレースホルダで、教材本体は未執筆。

## 講座の概要

- **到達目標 (canDo)**: Node.js で TypeScript を実行し、ファイルやネットワークを扱う小さなプログラムを書ける
- **前提講座**: `cli-basics` (クリアしていないと開けない)
- **後続 (スキルツリー)**: Git 入門のバックエンドの星が本講座のあとで開く (Git 側の `appearancePrerequisites`)。本線は `typescript-node-basics` → `rest-api-basics`。Python CI はサーバー TypeScript の先
- **参考にしたロードマップ**: roadmap.sh のバックエンドロードマップ

## 収録を予定している内容

- Node.js とは何か (ブラウザとの違い)
- npm スクリプトと TypeScript の実行
- ファイル操作と標準入出力
- HTTP サーバーの最小構成
- イベントループと非同期処理

## 執筆時のメモ

- 現在は M0 に準備中の案内 1 トピックだけを置いている。本執筆時は `packages/content/ADDING_COURSE.md` の手順で M1 以降を追加し、この CURRICULUM.md を確定版に書き換える。
- 前提講座を変える場合は course.json と本ファイルの両方を更新する (グラフ検査の対象)。
