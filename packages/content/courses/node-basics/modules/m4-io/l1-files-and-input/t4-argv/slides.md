---
id: 4-1-4
title: process.argvでコマンドライン引数を受け取る
takeaway: "コマンドラインで渡した引数は、process.argvの3番目以降に入る"
introduces: [process, argv]
requires: [Node.js, スクリプトファイル, 変数, console.log]
header: "Node.js 入門"
---

<!-- _class: lead -->

# 4-1-4
# process.argvでコマンドライン引数を受け取る

Node.js 入門 — Module 4 / レッスン4-1

<!-- ノート: 自作プログラムをコマンドとして使えるようにする回です。CLI 講座の $1 と同じ役割です。 -->

---

## なぜ必要か

- 対象のファイル名だけが違うプログラムを、何本も作ってしまう
- 実行のたびにコードを書き換えるのは、間違いのもと

<!-- ノート: つかみ。シェルスクリプトの $1 で扱った問題と同じ構図です。 -->

---

## 結論

**コマンドラインで渡した引数は、process.argvの3番目以降に入る**

- **process** — 実行中の自分自身の情報を持つオブジェクト
- **argv** — 渡された引数の一覧

<!-- ノート: 結論を先に言い切ります。3番目からになる理由を次のスライドで示します。 -->

---

## 最初の2つは決まっている

```js
// greet.js
const name = process.argv[2];
console.log(name + " さん、こんにちは");
```

```bash
node greet.js 佐藤
# argv[0] = nodeの場所  argv[1] = greet.jsの場所  argv[2] = 佐藤
```

<!-- ノート: 0 と 1 が埋まっている理由を説明すると、2 から始まることを暗記しなくて済みます。 -->

---

<!-- _class: summary -->

## まとめ

**コマンドラインで渡した引数は、process.argvの3番目以降に入る**

<!-- ノート: 結論の再掲だけ。次は引数ではなく環境から受け取ります。 -->
