---
id: 4-6-1
title: 分割代入
takeaway: "オブジェクトから必要なプロパティだけを、同じ名前の変数に取り出せる"
introduces: [分割代入]
requires: [オブジェクト, プロパティ, const, ドット記法]
header: "TypeScript入門"
---

<!-- _class: lead -->

# 4-6-1
# 分割代入

TypeScript入門 — Module 4 / レッスン4-6

<!-- ノート: Module 4の最後のレッスンです。まずは、次のトピックで必要になる書き方を1つ覚えます。 -->

---

## なぜ必要か

- `user.name`、`user.age` と何度も書くと、`user.`が繰り返しになる
- 使いたいのはプロパティの中身であって、オブジェクト自体ではない

<!-- ノート: つかみ。関数の中で同じオブジェクトのプロパティを5回参照すると、5回「user.」と書くことになる。読むときのノイズにもなる。 -->

---

## 結論

**オブジェクトから必要なプロパティだけを、同じ名前の変数に取り出せる**

- この書き方を分割代入と呼ぶ

<!-- ノート: 結論を先に言い切る。分割代入という言葉を定義する。左辺に中かっこを書くのが特徴で、3-3-1のオブジェクトリテラルと形が似ているが役割は逆(作るのではなく取り出す)。 -->

---

## 最小のコード

```ts
const user = { name: "田中", age: 28 };

const { name, age } = user;

console.log(name); // => "田中"
console.log(age); // => 28
```

- 中かっこの中に書いた名前の変数ができる

<!-- ノート: プロパティ名と同じ名前の変数が作られる。順番は関係なく、名前で対応する。必要なものだけ書けばよく、全部書く必要はない。 -->

---

## 必要なものだけ取り出せる

```ts
const user = { name: "田中", age: 28, email: "a@example.com" };

const { name } = user; // nameだけ取り出す
console.log(name); // => "田中"
```

<!-- ノート: 対比枠。3つあるうち1つだけを取り出している。配列でも同じことができるが(角かっこを使い、順番で対応する)、実務ではオブジェクトの分割代入のほうが圧倒的に多い。 -->

---

<!-- _class: summary -->

## まとめ

**オブジェクトから必要なプロパティだけを、同じ名前の変数に取り出せる**

<!-- ノート: 結論の再掲だけ。この書き方は関数の引数でも使えると引きを作って締める。 -->
