---
id: 7-3-1
title: extendsで継承する
takeaway: "継承すると、親クラスのプロパティとメソッドを子クラスが受け継ぐ"
introduces: [継承, 親クラス, 子クラス, protected]
requires: [クラス, プロパティ, メソッド, 型引数の制約, new, private, アクセス修飾子]
header: "TypeScript入門"
---

<!-- _class: lead -->

# 7-3-1
# extendsで継承する

TypeScript入門 — Module 7 / レッスン7-3

<!-- ノート: レッスン7-3は継承です。強力ですが乱用されやすい機能なので、使いどころも一緒に伝えます。 -->

---

## なぜ必要か

- 「通常会員」と「プレミアム会員」は、共通部分が9割で違いは1割
- クラスを丸ごとコピーすると、共通部分の修正が二重になる

<!-- ノート: つかみ。5-5-1の交差型で「共通項目 + 個別項目」を扱ったのと同じ動機。あちらは型の話、こちらは処理も含めた話。 -->

---

## 結論

**継承すると、親クラスのプロパティとメソッドを子クラスが受け継ぐ**

```
class 子クラス extends 親クラス { ... }
```

<!-- ノート: 結論を先に言い切る。3語を定義する。extendsは6-1-4の型引数の制約でも出てきたが、あちらは「〜を満たす型に限る」、こちらは「〜を受け継ぐ」。同じ単語で別の意味なので、混同しないよう明示する。 -->

---

## 最小のコード

```ts
class Member {
  constructor(protected name: string) {}

  greet(): string {
    return `${this.name}さん、こんにちは`;
  }
}

class PremiumMember extends Member {}

const p = new PremiumMember("田中");
console.log(p.greet()); // => "田中さん、こんにちは"
```

<!-- ノート: 子クラスの中は空なのに、親のメソッドが使えている。protectedは「自分と子クラスからは見えるが、外からは見えない」という3つ目のアクセス修飾子。privateだと子から見えないので、継承前提ならprotectedにする。 -->

---

## 親の持ち物を子が受け継ぐ

![w:950](assets/inheritance-tree.svg)

<!-- ノート: 親の持ち物が子にそのまま流れてくる。子は独自のプロパティやメソッドを足せる。ただし継承は結びつきが強く、親を変えると子すべてに影響するので乱用は禁物。まず5-5-1の交差型や、後で学ぶインターフェースで足りないかを考える。 -->

---

<!-- _class: summary -->

## まとめ

**継承すると、親クラスのプロパティとメソッドを子クラスが受け継ぐ**

<!-- ノート: 結論の再掲だけ。子で独自のプロパティを持つとどうなるか、という問いを残して締める。 -->
