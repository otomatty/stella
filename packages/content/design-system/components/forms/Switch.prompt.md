**Switch** — binary toggle; on-state fills with the brand gradient.

```jsx
<Switch checked={on} onChange={setOn} label="メール通知を受け取る" />
<Switch size="sm" checked disabled />
```

`size`: sm · md. Provide `label` for an inline clickable label, or use bare for table cells. Keyboard: Space/Enter toggles.
